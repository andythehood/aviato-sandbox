name: Deploy changed apps → GCS (sequential)

on:
  push:
    branches: [ "main" ]
    paths:
      - "web/**"
  workflow_dispatch:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.detect.outputs.apps }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Detect changed apps
        id: detect
        run: |
          echo "Detecting changed apps..."
          git fetch origin main

          # List changed files since last main commit
          CHANGED_PATHS=$(git diff --name-only origin/main...HEAD | grep '^web/' || true)

          if [ -z "$CHANGED_PATHS" ]; then
            echo "No app changes detected."
            echo "apps=[]" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Extract unique app directories (e.g. web/core, web/admin)
          APPS=$(echo "$CHANGED_PATHS" | cut -d'/' -f2 | sort -u | jq -R -s -c 'split("\n")[:-1]')
          echo "Detected apps: $APPS"
          echo "apps=$APPS" >> $GITHUB_OUTPUT

  build-and-deploy:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.apps != '[]' && needs.detect-changes.outputs.apps != '' }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1  # sequential deploys
      matrix:
        app: ${{ fromJson(needs.detect-changes.outputs.apps) }}

    env:
      NODE_VERSION: 22
      GCP_PROJECT: ${{ vars.GCP_PROJECT }}
      GCS_BUCKET: ${{ vars.GCS_BUCKET_APPS }}

    steps:
      - uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: 📦 Setup PNPM (no Corepack)
        run: |
          npm install -g pnpm@10
          pnpm --version

      - name: 📚 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build React app
        working-directory: web/${{ matrix.app }}
        run: |
          echo "Building app: ${{ matrix.app }}"
          pnpm build

      - name: ☁️ Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: 🧰 Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT }}
        #   export_default_credentials: true

      - name: 📤 Upload build to GCS
        uses: google-github-actions/upload-cloud-storage@v3
        with:
          path: web/${{ matrix.app }}/dist/**
          destination: gs://${{ env.GCS_BUCKET }}/${{ matrix.app }}/
          parent: false
        #   overwrite: true

      - name: 🌎 Invalidate CDN cache for this app
        run: |
          echo "Invalidating CDN cache for /${{ matrix.app }}/*"
          gcloud compute backend-buckets invalidate-cdn-cache "${{ matrix.app }}-backend" \
            --path "/${{ matrix.app }}/*"

